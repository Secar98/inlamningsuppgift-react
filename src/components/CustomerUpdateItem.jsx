import React from "react";

export default function CustomerUpdateItem({ data, label, input, type }) {
  console.log(data);

  return (
    <div>
      <label>{label}</label>
      <input />
    </div>
  );
}
